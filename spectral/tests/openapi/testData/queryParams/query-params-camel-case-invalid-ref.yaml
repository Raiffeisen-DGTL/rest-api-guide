paths:
  /users:
    get:
      parameters:
        - name: user_id
          in: query
          schema:
            type: string
        - name: first_name
          in: query
          schema:
            type: string
        - name: page_size
          in: query
          schema:
            type: integer