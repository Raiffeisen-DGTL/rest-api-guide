openapi: 3.0.0
info:
  title: Test Spec with Response Ref - Invalid
  version: 0.0.1
paths:
  /users:
    post:
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserRequest'
      responses:
        '200':
          $ref: './response-invalid.yaml#/UserCreate'
components:
  schemas:
    UserRequest:
      type: object
      properties:
        name:
          type: string
        email:
          type: string