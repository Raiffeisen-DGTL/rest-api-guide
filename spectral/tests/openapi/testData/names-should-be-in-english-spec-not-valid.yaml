openapi: 3.0.3
info:
  title: Test Spec with Refs
  version: 0.0.1
paths:
  /v1/api/test:
    get:
      tags:
        - testing endpoint
      summary: Для тестирования правил
      operationId: getTestV1
      responses:
        '200':
          description: 'some description'
          content:
            application/json:
              schema:
                $ref: './names-should-be-in-english-external-spec.yaml#/components/schemas/пользователь'
  /v2/api/test:
    get:
      tags:
        - testing endpoint
      summary: Для тестирования правил
      operationId: getTestV2
      responses:
        '200':
          description: 'some description'
          content:
            application/json:
              schema:
                $ref: './names-should-be-in-english-external-spec.yaml#/components/schemas/Status'
