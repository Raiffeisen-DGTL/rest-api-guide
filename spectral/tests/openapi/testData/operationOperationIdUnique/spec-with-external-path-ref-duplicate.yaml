openapi: 3.0.3
info:
  title: Test Spec with External Path References - Duplicate OperationId
  version: 0.0.2
paths:
  /v1/users:
    $ref: './external-path-with-operation-id.yaml'
  /v1/posts:
    get:
      operationId: getUsers  # Same operationId as in the external reference
      responses:
        '200':
          description: Success