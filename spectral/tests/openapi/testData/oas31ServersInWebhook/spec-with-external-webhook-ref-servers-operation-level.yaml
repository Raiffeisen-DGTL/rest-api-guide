openapi: 3.1.0
info:
  title: Test Spec with External Webhook Ref with Operation Level Servers
  version: 0.0.1
webhooks:
  myWebhook:
    $ref: './external-webhook-with-servers-operation-level.yaml#/webhooks/externalWebhook'
components:
  schemas:
    User:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string