openapi: 3.0.3
info:
  title: Test Spec with Valid External Body Fields Model
  version: 0.0.2
paths:
  /test:
    post:
      requestBody:
        content:
          application/json:
            schema:
              $ref: './external-body-fields-valid-model.yaml#/components/schemas/ValidExternalModel'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: './external-body-fields-valid-model.yaml#/components/schemas/ValidExternalModel'