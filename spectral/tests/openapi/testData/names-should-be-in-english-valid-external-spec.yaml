components:
  schemas:
    User:  # English name
      type: object
      properties:
        firstName:  # English name
          type: string
        lastName:  # English name
          type: string
    Status:
      type: string
      enum:
        - active
        - inactive
        - pending