asyncapi: '2.0.0'
info:
  title: Test Spec with Internal Ref
  version: '0.0.1'
channels:
  test.channel:
    subscribe:
      message:
        $ref: '#/components/schemas/TestModel'
components:
  schemas:
    TestModel:
      type: object
      properties:
        id:
          type: string