rules:
  oas3-api-servers:
    description: Servers должны быть заполнены
    formats: [ oas3_0 ]
    severity: error
    resolved: false
    given: '$'
    then:
      field: 'servers'
      function: schema
      functionOptions:
        dialect: 'draft7'
        schema:
          items:
            type: 'object'
          minItems: 1
          type: 'array'
