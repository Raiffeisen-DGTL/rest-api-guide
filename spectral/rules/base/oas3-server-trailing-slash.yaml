rules:
  oas3-server-trailing-slash:
    description: Servers не должны заканчиваться на символ /
    severity: info
    formats: [oas3]
    given: '$.servers[*].url'
    then:
      function: pattern
      functionOptions:
        notMatch: './$'