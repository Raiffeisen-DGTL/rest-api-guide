rules:
  no-eval-in-markdown:
    description: Блок description и title не должен содержать eval()
    resolved: false
    severity: error
    given: '$..[description,title]'
    then:
      function: pattern
      functionOptions:
        notMatch: "eval\\("